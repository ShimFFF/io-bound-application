spring:
  datasource:
    url: jdbc:postgresql://10.140.0.3/postgresql
    username: postgresql
    password: postgrespassword
  jpa:
    show-sql: true
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
      ddl-auto: update
  rabbitmq:
    host: 10.146.0.2
    port: 5672
    username: guest
    password: guest